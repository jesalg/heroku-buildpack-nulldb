#!/bin/sh

echo "exporting DB_ADAPTER='nulldb'"
export DB_ADAPTER='nulldb'
  
if [ -z "$DATABSE_URL" ]; then
  echo "exporting DATABASE_URL='nulldb://nohost'"
  export DATABASE_URL='nulldb://nohost'
fi
